apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: chiloe-flora
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  type: ClusterIP
