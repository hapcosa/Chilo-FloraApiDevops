apiVersion: v1
kind: Service
metadata:
  name: especies-api-service
  namespace: chiloe-flora
  labels:
    app: especies-api
spec:
  selector:
    app: especies-api
  ports:
    - port: 80
      targetPort: 9080
      protocol: TCP
      name: http
  type: ClusterIP